// eslint-disable-next-line @typescript-eslint/explicit-function-return-type
const ComponentSwitcher = ({ list, index }: { list: any, index: number }): any => list[index]

export default ComponentSwitcher
